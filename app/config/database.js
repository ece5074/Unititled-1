const adminInfo = {
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'testschema',

    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
};

const testInfo = {
    host: 'localhost',
    user: 'root',
    password: 'root',
    database: 'testschema',

    waitForConnections: true,
    connectionLimit: 10,
    queueLimit: 0
};